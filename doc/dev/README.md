# 開發計畫區 (Development Plans)

本目錄存放功能開發前的規劃文件。

## 開發流程規範
1. **事前討論**：新功能開發前先釐清需求與實作邊界。
2. **撰寫計畫**：在 `doc/dev/` 底下建立 `plan_<功能名稱>.md` 文件，詳細記錄架構設計、異動檔案與驗證計畫。
3. **對齊執行**：開發過程嚴格參照對應的 `plan_<功能名稱>.md`。
4. **回寫規格**：功能實作與驗收完成後，必須將正式功能規格與行為回寫至 `doc/spec/` 的對應章節中。
